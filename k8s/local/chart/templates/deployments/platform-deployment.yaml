apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-service
  labels:
    {{- include "tripplanning.labels" . | nindent 4 }}
    app.kubernetes.io/component: platform-service
spec:
  replicas: {{ .Values.services.platform.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: platform-service
      {{- include "tripplanning.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: platform-service
        {{- include "tripplanning.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: platform-service
          image: "{{ .Values.services.platform.image.repository }}:{{ .Values.services.platform.image.tag }}"
          imagePullPolicy: {{ .Values.localDev.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.platform.port }}
          envFrom:
            - configMapRef:
                name: platform-service-config
{{- range .Values.services.platform.secretRefs }}
            - secretRef:
                name: {{ . }}
{{- end }}
          readinessProbe:
            httpGet:
              path: {{ .Values.services.platform.readinessPath }}
              port: {{ .Values.services.platform.port }}
            initialDelaySeconds: {{ .Values.services.platform.bootstrap.readinessInitialDelaySeconds }}
            periodSeconds: {{ .Values.services.platform.bootstrap.readinessPeriodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.services.platform.livenessPath }}
              port: {{ .Values.services.platform.port }}
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: {{ .Values.services.platform.resources.requests.cpu }}
              memory: {{ .Values.services.platform.resources.requests.memory }}
            limits:
              cpu: {{ .Values.services.platform.resources.limits.cpu }}
              memory: {{ .Values.services.platform.resources.limits.memory }}
