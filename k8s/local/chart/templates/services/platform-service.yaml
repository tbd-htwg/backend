apiVersion: v1
kind: Service
metadata:
  name: platform-service
  labels:
    {{- include "tripplanning.labels" . | nindent 4 }}
    app.kubernetes.io/component: platform-service
spec:
  selector:
    app.kubernetes.io/component: platform-service
    {{- include "tripplanning.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.services.platform.port }}
      targetPort: {{ .Values.services.platform.port }}
